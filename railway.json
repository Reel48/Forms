{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "railpack"
  },
  "deploy": {
    "startCommand": "if [ -d \"/app/backend\" ]; then cd /app/backend && /app/.venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}; else /app/.venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}; fi",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}

